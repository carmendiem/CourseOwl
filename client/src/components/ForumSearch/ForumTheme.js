import { createTheme } from '@mui/material/styles';

const forumTheme = createTheme({
    typography: {
      h2: {
        fontFamily: 'Paradose, Arial, sans-serif',
        fontSize: '6rem'
      },
      fontFamily: 'Arial, sans-serif', 
    },
  });


export default forumTheme;